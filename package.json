{
    "name": "blt-byte-chatbot",
    "version": "0.1.0",
    "description": "BLT AI Chatbot - Python AI worker for bug bounty assistance",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "pytest && vitest run",
        "test:js": "vitest run"
    },
    "devDependencies": {
        "wrangler": "^4.46.0",
        "vitest": "^1.0.0",
        "jsdom": "^24.0.0",
        "@vitest/coverage-v8": "^1.0.0"
    }
}